import numpy as np
Array =np.array([4,3,9,3,1])
n=len(Array)
swap=0
for i in range(0,n-1):
    for j in range(0,n-1-i):
        if(Array[j]>Array[j+1]):
            swap=swap+1
            temp=Array[j]
            Array[j]=Array[j+1]
            Array[j+1]=temp



print("Total swap",swap,Array)
